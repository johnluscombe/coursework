<h1>New User</h1><br>

<%= render 'shared/errors', object: @user %>

<%= form_for @user do |form| %>
<%= render partial: 'fields', locals: { f: form } %>

<%= form.submit 'Create new user', class: 'btn btn-default' %>
<% end %>